package lab_03;

public class BubbleSort {
    public static void main(String[] args) {
        int[] myArr = {2, 4, 5, 0, 1, 0};
        /*
        * unsortedPosition = arrL - 1
        * loop over the arr till the index > unsortedPosition && unsortedPosition > 0
        *   left > right (arr[i] > arr
         */
    }
}
